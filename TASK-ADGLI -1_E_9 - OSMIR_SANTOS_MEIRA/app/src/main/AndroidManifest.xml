<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._2"
        tools:targetApi="31">

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MinhasReservasActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ListaLaboratoriosActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ListaEstacoesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AgendamentoActivity"
            android:exported="false" />
        
        <!-- Mantendo a referência a OpcoesReservaActivity caso seja usada -->
        <activity
            android:name=".ui.OpcoesReservaActivity"
            android:exported="false" />

    </application>

</manifest>
